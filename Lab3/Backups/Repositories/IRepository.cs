using Backups.Inter;
using Zio;

namespace Backups;

/*public interface IRepository
{
    string FullName { get; }
    Stream OpenStream(string path);
    void SaveTo(string path, Stream stream);
    void AddBackupTask(IBackupTask backupTask);

    IReadOnlyCollection<IBackupTask> GetBackupTasks();
    bool Exists(string path);
    bool FileExists(string fileName);
    bool DirectoryExists(string folderName);
}*/